# APK downloads

Place the Android APK here as `ggapp.apk` so the /download page can serve it
at `/downloads/ggapp.apk` (see src/features/marketing/DownloadScreen.tsx).

Built via `eas build --platform android` (production profile) in
gg-monorepo/apps/mobile, then copied here.
